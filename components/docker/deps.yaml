name: Docker
order: 5
requires:
  - essentials
check: docker --version
# apt packages are NOT listed here because they require Docker's official
# repository, which is added by setup.bash before installing them.
